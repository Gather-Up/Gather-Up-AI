name: dependency-maintenance (manual only)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This workflow is disabled; use deps-check instead."
